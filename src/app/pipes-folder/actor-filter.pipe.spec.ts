import { ActorFilterPipe } from './actor-filter.pipe';

describe('ActorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ActorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
